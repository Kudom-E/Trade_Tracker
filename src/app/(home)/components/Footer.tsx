import React from "react";

type Props = {};

const Footer = (props: Props) => {
  return (
    <footer className="px-[5%] lg:px-[15%] flex justify-center">
      © Copyright
    </footer>
  );
};

export default Footer;
